using UnityEngine;

public class esoquecoñoes : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        PlayerPrefs.SetInt("parrafos", 4);
    }

    // Update is called once per frame
    void Update()
    {

    }
}
